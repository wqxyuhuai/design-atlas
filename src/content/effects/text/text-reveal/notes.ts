export const notes = `Use case: hero titles, case-study titles, banner copy, and section openers.

Design points: the animation supports clarity first. Blur should be low enough that the text remains readable quickly. Direction is useful when matching nearby layout movement.

Implementation note: keying by word and index replays the animation when the text changes.

Reuse guidance: keep delay values conservative so the headline still feels editorial and not theatrical.`;
