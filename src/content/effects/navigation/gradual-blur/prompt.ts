export const prompt = `Create a reusable gradual edge blur overlay for navigation and scroll containers. Use multiple absolutely positioned layers with CSS backdrop-filter and mask-image gradients. Expose controls for edge position, blur strength, height, layer count, opacity, curve, exponential progression, and z-index. Default to a top navigation edge with strength 5 and 4 blur layers.`;
