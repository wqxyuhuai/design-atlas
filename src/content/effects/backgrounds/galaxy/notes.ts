export const notes = `Original website: https://reactbits.dev/backgrounds/galaxy

Use case: interactive star-field backgrounds for hero surfaces, launch pages, immersive product pages, and dark visual sections.

Design points: the background works best behind sparse foreground content. Keep density, glow, and saturation restrained when text sits over it; increase hueShift and saturation only for more expressive campaign pages.

Parameter notes: density controls star count, glowIntensity controls flare energy, saturation and hueShift shape color, twinkleIntensity controls shimmer, and mouseRepulsion plus repulsionStrength control pointer response. The default values follow the ReactBits Galaxy reference closely while exposing scalar controls that fit this site's workbench.`;
