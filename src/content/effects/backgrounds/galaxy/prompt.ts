export const prompt = `Create an interactive WebGL galaxy background for a dark product surface. Use layered procedural stars, subtle twinkle, configurable density, hue shift, glow intensity, automatic rotation, and mouse interaction with optional repulsion. Keep text legible by default and expose production-friendly controls for intensity, speed, saturation, and pointer behavior.`;
