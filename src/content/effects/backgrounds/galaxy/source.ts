export const source = `Source reference: ReactBits Galaxy background
Original website: https://reactbits.dev/backgrounds/galaxy

Implementation note: this Design Atlas entry adapts the public ReactBits Galaxy concept into the local effect registry and control model. It keeps the WebGL star-layer interaction model and attaches the original reference URL for traceability.

Dependency note: Galaxy uses ogl for the WebGL renderer, mesh, program, color, and triangle primitives.`;
