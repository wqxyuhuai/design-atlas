export const notes = `Use case: portfolio indexes, case-study lists, primary navigation, and large menu links.

Interaction: hover only activates the row under the pointer. Pointer velocity drives local sliced distortion, and the OPEN label follows with a small delay.

Click behavior: each row plays the masked exit and then re-enters, so the preview can validate entrance, exit, and hover without routing.

Accessibility: reduced-motion users get static readable text with the liquid layer and cursor label disabled.`;
